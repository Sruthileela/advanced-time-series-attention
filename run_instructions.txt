1. Install required libraries:
   pip install tensorflow pandas numpy scikit-learn matplotlib

2. Ensure dataset CSV is placed in the project root folder.

3. Run the training script:
   python train.py

4. Results and forecast plots will be generated automatically.
