1. Install Python 3.8 or above

2. Install required libraries:
   pip install -r requirements.txt

3. Place your dataset as data.csv
   Required columns:
   - Date
   - Value

4. Run the training script:
   python train.py

5. Output:
   - Model training logs
   - RMSE and MAE values
   - Forecast visualization plot
